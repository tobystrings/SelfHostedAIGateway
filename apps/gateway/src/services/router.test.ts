import {describe,it,expect} from 'vitest';import {RoutingEngine} from './router.js';import {ModelRegistry} from './model-registry.js';import {ProviderRegistry} from './provider-registry.js';describe('router',()=>{it('filters tools capability',()=>{const m=new ModelRegistry();m.setMany([{provider:'p',id:'plain',enabled:true,capabilities:{textInput:true}},{provider:'p',id:'tools',enabled:true,capabilities:{toolCalling:true}}]);const r=new RoutingEngine(m,new ProviderRegistry());expect(r.route({messages:[],tools:[{name:'x',parameters:{}}]}).model).toBe('tools')})})
