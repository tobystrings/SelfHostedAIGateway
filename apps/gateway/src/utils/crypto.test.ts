import {describe,it,expect} from 'vitest';import {encryptSecret,decryptSecret,passwordHash,passwordVerify} from './crypto.js';describe('crypto',()=>{it('round trips encrypted data',()=>{const k=Buffer.alloc(32,1).toString('base64');expect(decryptSecret(encryptSecret({a:1},k),k)).toEqual({a:1})});it('hashes passwords',()=>{const h=passwordHash('abc');expect(passwordVerify('abc',h)).toBe(true);expect(passwordVerify('no',h)).toBe(false)})})
