import type {ButtonHTMLAttributes,PropsWithChildren} from 'react';export function Card(p:PropsWithChildren<{className?:string}>){return <section className={`card ${p.className??''}`}>{p.children}</section>}export function Button(p:ButtonHTMLAttributes<HTMLButtonElement>){return <button {...p} className={`primary ${p.className??''}`}/>}export function Field(p:PropsWithChildren<{label:string}>){return <label className="field"><span>{p.label}</span>{p.children}</label>}
