import {useEffect,useState} from 'react';import {api} from '../lib/api';import {Card} from '../components/Ui';export function Dashboard(){const [d,setD]=useState<any>({});useEffect(()=>{api('/api/admin/dashboard').then(setD)},[]);return <><h1>Dashboard</h1><div className="grid"><Card><small>Requests · 30d</small><b>{d.requests??0}</b></Card><Card><small>Tokens · 30d</small><b>{String(d.tokens??0)}</b></Card><Card><small>Estimated spend</small><b>${Number(d.spend??0).toFixed(4)}</b></Card></div></>}
