import {useEffect,useState} from 'react';import {api} from '../lib/api';import {Card} from '../components/Ui';export function Routing(){const [d,setD]=useState<any>({policies:[],budgets:[],rateLimits:[]});useEffect(()=>{api('/api/admin/routing').then(setD)},[]);return <><h1>Routing & Controls</h1><Card><h2>Policies</h2><pre>{JSON.stringify(d.policies,null,2)}</pre></Card><Card><h2>Budgets</h2><pre>{JSON.stringify(d.budgets,null,2)}</pre></Card><Card><h2>Rate limits</h2><pre>{JSON.stringify(d.rateLimits,null,2)}</pre></Card></>}
