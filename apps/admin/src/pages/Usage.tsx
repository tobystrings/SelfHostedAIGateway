import {useEffect,useState} from 'react';import {api} from '../lib/api';export function Usage(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api('/api/admin/usage').then(setRows)},[]);return <><h1>Usage</h1><div className="tablewrap"><table><thead><tr><th>Time</th><th>Provider/model</th><th>Input</th><th>Output</th><th>Cost</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{new Date(r.created_at).toLocaleString()}</td><td>{r.provider}/{r.model}</td><td>{r.input_tokens}</td><td>{r.output_tokens}</td><td>${Number(r.estimated_cost_usd).toFixed(5)}</td></tr>)}</tbody></table></div></>}
